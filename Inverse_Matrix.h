#ifndef Inverse_Matrix.h
#define Inverse_Matrix.h
#include <math.h>
#include <vector>
using namespace std;

double det(vector<double>);

vector<vector<double>> inverse(vector<vector<double>>);

#endif
